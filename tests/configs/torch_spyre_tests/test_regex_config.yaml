test_suite_config:
  files:
    - path: ${TORCH_DEVICE_ROOT}/tests/test_regex.py
      unlisted_test_mode: skip
      tests:
        - names:
            - test_schema_argument_extraction
          mode: mandatory_success